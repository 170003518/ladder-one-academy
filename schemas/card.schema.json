{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ladderoneacademy.local/schemas/card.schema.json",
  "title": "L1A Note Card",
  "description": "One printable, flippable note card. Cards are a first-class product, not an export (docs/master-plan.md 6). A card is generated from its parent concept - concept.card_data holds the structured content, this record holds the rendered two sides plus everything the print layout needs. Layout is fixed by brand/print.css: L1A stamp top-left, tier color bar down the left edge, type label top-right, footer line across the bottom.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "id", "concept_id", "type", "front", "back", "footer"],

  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1.0.0",
      "default": "1.0.0"
    },

    "id": {
      "description": "Card ID: the parent concept ID plus -Cnn, e.g. EMT-02-03-04-C01.",
      "type": "string",
      "pattern": "^(EMT|FIRE|MEDIC)-[0-9]{2}-[0-9]{2}-[0-9]{2}-C[0-9]{2}$"
    },
    "concept_id": {
      "description": "Parent concept. The card's content is generated from that concept's key_points and card_data so the deck can never drift from the lesson.",
      "$ref": "#/$defs/concept_id"
    },
    "type": {
      "description": "One of the seven card types. Must be listed in the parent concept's cards[], and the matching concept.card_data sub-object should exist for every type except 'concept'.",
      "type": "string",
      "enum": ["concept", "drug", "skill", "numbers", "algorithm", "mnemonic", "compare"]
    },

    "front": {
      "description": "The prompt side. Kept short enough to read at a glance on a 3x5.",
      "type": "object",
      "additionalProperties": false,
      "required": ["title"],
      "properties": {
        "title": {
          "description": "The card's headline - the one idea, the drug name, the skill name.",
          "type": "string",
          "minLength": 2,
          "maxLength": 80
        },
        "summary": {
          "description": "One-sentence framing under the title. Optional; a bare title front is fine for recall drilling.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "back": {
      "description": "The answer side. For structured types the renderer builds this from concept.card_data; body is the prose fallback and the whole of a concept card.",
      "type": "object",
      "additionalProperties": false,
      "required": ["body"],
      "properties": {
        "body": {
          "type": "string",
          "minLength": 2,
          "maxLength": 1200
        },
        "why_it_matters": {
          "description": "The one-line 'why this is on the card' that every concept card carries.",
          "type": "string",
          "maxLength": 200
        }
      }
    },

    "high_yield": {
      "description": "Known exam favorite. Prints the Signal Yellow high-yield flag. Normally inherited from the parent concept.",
      "type": "boolean",
      "default": false
    },
    "number": {
      "description": "Position within its curated deck, for ordering a printed set and putting it back in order after a shuffle.",
      "type": "integer",
      "minimum": 1
    },
    "print_size": {
      "description": "Card stock size. 3x5 in is the locked primary (decisions.md); 4x6 and A6 are optional layouts. The print engine tiles whichever is chosen onto Letter or A4 with cut guides and duplex-aligned backs.",
      "type": "string",
      "enum": ["3x5", "4x6", "A6"],
      "default": "3x5"
    },

    "footer": {
      "description": "The printed footer line and the left-edge color bar. Cards use the SHORT form - Rung N - Tier - Module N Name - with no 'Ladder One Academy' prefix, because the stamp above already reads L1A and the full string wraps to two lines at 3x5 in. Tests and worksheets keep the full form. In black and white the tier bar is told apart by texture rather than color: EMT hatched, Fire solid, Paramedic dotted.",
      "type": "object",
      "additionalProperties": false,
      "required": ["tier", "rung", "module_number", "module_name"],
      "properties": {
        "tier": {
          "description": "Tier label as printed.",
          "type": "string",
          "enum": ["EMT", "Fire", "Paramedic"]
        },
        "rung": {
          "description": "Rung number: 1 EMT, 2 Fire, 3 Paramedic. Also selects the color bar - star white, ember, line blue.",
          "type": "integer",
          "minimum": 1,
          "maximum": 3
        },
        "module_number": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20
        },
        "module_name": {
          "description": "Short module title, e.g. 'Airway'.",
          "type": "string",
          "minLength": 2,
          "maxLength": 32
        }
      }
    },

    "verify": {
      "description": "True = content on this card is unconfirmed. A flagged card prints with a verify mark and is kept out of the spaced-repetition deck: drilling an unverified fact is worse than not drilling it, because repetition is what makes it stick. Inherited from the parent concept unless set explicitly.",
      "type": "boolean",
      "default": false
    },
    "verify_notes": {
      "description": "What needs checking. Expected whenever verify is true.",
      "type": "string",
      "maxLength": 600
    },

    "media": {
      "description": "Media id from the parent concept's media[] to print on the card - a diagram or strip. Must be print_safe.",
      "$ref": "#/$defs/media_id"
    },
    "side": {
      "description": "Which side the media sits on.",
      "type": "string",
      "enum": ["front", "back"],
      "default": "back"
    }
  },

  "allOf": [
    {
      "description": "Naming a side without media is meaningless.",
      "dependentRequired": { "side": ["media"] }
    },
    {
      "description": "If it is flagged for verification, say what needs verifying.",
      "if": { "properties": { "verify": { "const": true } }, "required": ["verify"] },
      "then": { "required": ["verify_notes"] }
    }
  ],

  "$defs": {
    "concept_id": {
      "type": "string",
      "pattern": "^(EMT|FIRE|MEDIC)-[0-9]{2}-[0-9]{2}-[0-9]{2}$"
    },
    "media_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(_[a-z0-9]+)*$"
    }
  }
}
